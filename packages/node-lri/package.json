{
  "name": "node-lri",
  "version": "0.1.0",
  "description": "Node.js SDK for Liminal Resonance Interface (LRI)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Tests not yet implemented\" && exit 0",
    "lint": "echo \"Linting not yet configured\" && exit 0",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["lri", "semantic", "context", "ai", "layer8"],
  "author": "LRI Contributors",
  "license": "MIT",
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  },
  "peerDependencies": {
    "express": "^4.18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
